{
  "name": "example",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p ./",
    "build:bun": "bun build ./src --outdir=lib --minify --target=node",
    "lint": "eslint --ext .js,.ts src"
  },
  "author": "h3poteto",
  "license": "MIT",
  "dependencies": {
    "megalodon": "*",
    "typescript": "5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.6.3",
    "log4js": "^6.9.1"
  }
}
